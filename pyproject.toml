[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Developer log"
authors = [{name = "Toon Daemen", email = "toon.daemen@flandersmake.be"}]
dynamic = ["version", "description"]
